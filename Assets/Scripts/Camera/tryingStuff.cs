using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//IGNORE THIS SCRIPT
public class tryingStuff : MonoBehaviour {
	/*
	public bool top;

	Vector3 startPos;
	float endPosY;

	void Start() {
		if (top) {
			//startPos = new Vector3 (62.7f, 200f, 0f);
			startPos = transform.position;
			endPosY = 35.3f;
		} else {
			//startPos = new Vector3 (31f, -200f, 0f);
			startPos = transform.position;
			endPosY = -36.8f;
		}
		transform.position = startPos;
	}

	void Update() {
		if (Input.GetKeyDown(KeyCode.B)) {
			Bite ();
		}
	}

	public float speed;

	public void Bite() {
		if (top) {
			if (transform.position.y > 35.3f) {
				transform.position.y -= 0.1f * Time.deltaTime * speed;
			}
		} else {
			if (transform.position.y < -36.8f) {
				transform.position.y += 0.1f * Time.deltaTime * speed;
			}
		}
	}
	*/
}